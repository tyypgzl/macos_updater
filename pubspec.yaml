name: desktop_updater
description: "A headless Flutter desktop OTA update engine for macOS. Downloads only changed files via Blake2b delta diffing. Bring your own backend via the abstract UpdateSource interface."
version: 2.0.0
homepage: https://github.com/MarlonJD/flutter_desktop_updater
repository: https://github.com/MarlonJD/flutter_desktop_updater

platforms:
  macos:
  windows:
  linux:

topics:
  - updater
  - desktop-updater
  - ota
  - auto-updater
  - flutter-desktop

environment:
  sdk: ">=3.7.0 <4.0.0"
  flutter: ">=3.29.0"

dependencies:
  archive: ^4.0.9
  cryptography_flutter_plus: ^3.0.0
  cryptography_plus: ^3.0.0
  flutter:
    sdk: flutter
  http: ^1.6.0
  path: ^1.9.1
  plugin_platform_interface: ^2.1.8
  pubspec_parse: ^1.5.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec
# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      linux:
        pluginClass: DesktopUpdaterPlugin
      macos:
        pluginClass: DesktopUpdaterPlugin
      windows:
        pluginClass: DesktopUpdaterPluginCApi
  # To add assets to your plugin package, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/to/asset-from-package
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images
  # To add custom fonts to your plugin package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/to/font-from-package
