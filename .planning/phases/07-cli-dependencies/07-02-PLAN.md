---
phase: 07-cli-dependencies
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
autonomous: true
requirements: [CLI-03, CLI-04, CLI-05, CLI-06]

must_haves:
  truths:
    - "pubspec.yaml version is 2.0.0"
    - "SDK constraint is sdk: '>=3.7.0 <4.0.0' and flutter: '>=3.29.0'"
    - "cryptography_plus is ^3.0.0 and cryptography_flutter_plus is ^3.0.0"
    - "http is ^1.6.0, archive is ^4.0.9, path is ^1.9.1, plugin_platform_interface is ^2.1.8"
    - "args dependency is removed from pubspec.yaml"
    - "flutter_lints is ^6.0.0 in dev_dependencies"
    - "dart pub get succeeds with no version conflicts"
  artifacts:
    - path: "pubspec.yaml"
      provides: "Updated package manifest for v2.0.0"
      contains: "version: 2.0.0"
  key_links:
    - from: "pubspec.yaml"
      to: "pubspec.lock"
      via: "dart pub get"
      pattern: "cryptography_plus.*3\\."
---

<objective>
Update pubspec.yaml to reflect v2.0.0: bump the package version, update the SDK/Flutter environment constraints to Dart 3.7+/Flutter 3.29+, update all dependencies to their latest compatible versions, and remove the unused `args` dependency.

Purpose: The package's pubspec has been at v1.4.0 with a Dart ^3.6.0 floor. v2.0.0 requires Dart 3.7+ (wildcard variables, new formatter), and all dependencies should be at current versions to reduce consumer conflicts.
Output: Updated pubspec.yaml with correct version, SDK constraint, and dependency versions. pubspec.lock regenerated by dart pub get.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@.planning/phases/07-cli-dependencies/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update pubspec.yaml — version, SDK, and all dependencies</name>
  <files>pubspec.yaml</files>
  <read_first>pubspec.yaml</read_first>
  <action>
Update pubspec.yaml with all v2.0.0 changes (per CLI-03, CLI-04, CLI-05, CLI-06):

1. **Version** (line 3): Change `version: 1.4.0` to `version: 2.0.0`

2. **Environment** (CLI-03): Replace the entire environment block:
   ```yaml
   environment:
     sdk: ">=3.7.0 <4.0.0"
     flutter: ">=3.29.0"
   ```

3. **Dependencies** — replace the entire dependencies block:
   ```yaml
   dependencies:
     archive: ^4.0.9
     cryptography_flutter_plus: ^3.0.0
     cryptography_plus: ^3.0.0
     flutter:
       sdk: flutter
     http: ^1.6.0
     path: ^1.9.1
     plugin_platform_interface: ^2.1.8
     pubspec_parse: ^1.5.0
   ```
   Key changes vs current:
   - `archive`: ^4.0.2 → ^4.0.9 (CLI-05)
   - `args`: REMOVED entirely — not imported in any CLI file after Phase 7 simplification (CLI-06)
   - `cryptography_flutter_plus`: ^2.3.4 → ^3.0.0 (CLI-04)
   - `cryptography_plus`: ^2.7.1 → ^3.0.0 (CLI-04)
   - `http`: ^1.2.2 → ^1.6.0 (CLI-05)
   - `path`: ^1.9.0 → ^1.9.1 (CLI-05)
   - `plugin_platform_interface`: ^2.0.2 → ^2.1.8 (CLI-05)
   - `pubspec_parse`: ^1.5.0 → unchanged (already at latest)

4. **Dev dependencies** (CLI-05): Replace flutter_lints version:
   ```yaml
   dev_dependencies:
     flutter_lints: ^6.0.0
     flutter_test:
       sdk: flutter
   ```
   Change: `flutter_lints`: ^5.0.0 → ^6.0.0

5. Keep all other sections unchanged: name, description, homepage, repository, platforms, topics, flutter plugin section.

After editing pubspec.yaml, run:
```
cd /Users/tguzeldev/Desktop/dev/flutter_desktop_updater && dart pub get
```
This regenerates pubspec.lock and validates there are no version conflicts.

If `dart pub get` fails due to a version conflict, try lowering the constraint for the conflicting package by one minor version. The STACK.md versions are the target — use them unless pub explicitly rejects.
  </action>
  <verify>
    <automated>cd /Users/tguzeldev/Desktop/dev/flutter_desktop_updater && dart pub get</automated>
  </verify>
  <acceptance_criteria>
- `dart pub get` exits 0 with no errors
- pubspec.yaml `version:` is `2.0.0`
- pubspec.yaml `sdk:` is `">=3.7.0 <4.0.0"`
- pubspec.yaml `flutter:` environment is `">=3.29.0"`
- `args:` does not appear in dependencies section
- `cryptography_plus:` constraint starts with `^3`
- `cryptography_flutter_plus:` constraint starts with `^3`
- `flutter_lints:` constraint is `^6.0.0`
- `dart analyze lib/` exits 0 (no new lint failures introduced by flutter_lints 6.x)
  </acceptance_criteria>
  <done>pubspec.yaml reflects v2.0.0: correct version, Dart 3.7+/Flutter 3.29+ SDK constraint, all deps at latest compatible versions, args removed, dart pub get succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Verify dart analyze clean after dependency bump</name>
  <files></files>
  <read_first>analysis_options.yaml</read_first>
  <action>
Run `dart analyze` across the full package to verify flutter_lints 6.0.0 introduces no new lint failures.

```
cd /Users/tguzeldev/Desktop/dev/flutter_desktop_updater && dart analyze lib/ test/ bin/
```

flutter_lints 6.0.0 adds stricter rules over 5.0.0. Common new failures and fixes:
- `use_string_buffers`: replace string concatenation in loops with StringBuffer
- `avoid_dynamic_calls`: ensure dynamic is typed
- `unreachable_from_main`: dead code warnings (acceptable as info, not error)

If `dart analyze` reports errors (not just info/hints):
1. Read the flagged file
2. Apply the minimal fix (e.g., add an explicit type, rename a variable, add a missing override annotation)
3. Re-run `dart analyze` until exit code is 0

Do NOT suppress lints with `// ignore:` unless the lint rule is a false positive that cannot be fixed structurally. Prefer fixing the code.

Note: `analysis_options.yaml` already has `treat_warnings_as_errors: false` (if set). Only errors (exit code 1) block completion — warnings and hints are acceptable.
  </action>
  <verify>
    <automated>cd /Users/tguzeldev/Desktop/dev/flutter_desktop_updater && dart analyze lib/ test/ bin/ --fatal-infos=false</automated>
  </verify>
  <acceptance_criteria>
- `dart analyze` exits 0 (no errors)
- Any warnings/hints are pre-existing or acceptable (not introduced by this phase)
- No `// ignore:` comments added unless strictly unavoidable
  </acceptance_criteria>
  <done>Full package analysis clean after dependency bump — flutter_lints 6.0.0 rules satisfied</done>
</task>

</tasks>

<verification>
Final state checks:
```
grep "version:" /Users/tguzeldev/Desktop/dev/flutter_desktop_updater/pubspec.yaml | head -1
# Expected: version: 2.0.0

grep "sdk:" /Users/tguzeldev/Desktop/dev/flutter_desktop_updater/pubspec.yaml
# Expected: sdk: ">=3.7.0 <4.0.0"

grep "args:" /Users/tguzeldev/Desktop/dev/flutter_desktop_updater/pubspec.yaml
# Expected: no output

grep "cryptography_plus:" /Users/tguzeldev/Desktop/dev/flutter_desktop_updater/pubspec.yaml
# Expected: cryptography_plus: ^3.0.0
```
</verification>

<success_criteria>
- Package version is 2.0.0
- SDK constraint: Dart >=3.7.0 <4.0.0, Flutter >=3.29.0
- cryptography_plus and cryptography_flutter_plus at ^3.0.0
- http ^1.6.0, archive ^4.0.9, path ^1.9.1, plugin_platform_interface ^2.1.8
- args removed from dependencies
- flutter_lints at ^6.0.0
- dart pub get and dart analyze both clean
</success_criteria>

<output>
After completion, create `.planning/phases/07-cli-dependencies/07-02-SUMMARY.md`
</output>
