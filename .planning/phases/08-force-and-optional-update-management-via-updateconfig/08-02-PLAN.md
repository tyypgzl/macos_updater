---
phase: 08-force-and-optional-update-management-via-updateconfig
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - README.md
  - CHANGELOG.md
  - example/lib/app.dart
autonomous: true
requirements:
  - D-20
  - D-21
  - D-24

must_haves:
  truths:
    - "README.md documents isMandatory, minBuildNumber, and releaseNotes fields with a concrete code example showing the consumer switch pattern from D-24"
    - "CHANGELOG.md has a v2.1.0 section above v2.0.0 listing the 3 new fields and the engine auto-enforcement behavior"
    - "example/lib/app.dart demonstrates force vs optional update UI: force shows a non-dismissible dialog, optional shows a banner"
  artifacts:
    - path: "README.md"
      provides: "Force/optional update usage documentation"
      contains: "isMandatory"
    - path: "CHANGELOG.md"
      provides: "v2.1.0 release entry"
      contains: "2.1.0"
    - path: "example/lib/app.dart"
      provides: "Force vs optional UI demo"
      contains: "isMandatory"
  key_links:
    - from: "README.md force/optional section"
      to: "UpdateInfo.isMandatory field"
      via: "code snippet showing info.isMandatory check"
      pattern: "info\\.isMandatory"
    - from: "example/lib/app.dart"
      to: "checkForUpdate result"
      via: "switch on UpdateAvailable(:final info) then if (info.isMandatory)"
      pattern: "isMandatory"
---

<objective>
Update README, CHANGELOG, and example app to document and demonstrate the new force/optional update feature introduced in plan 08-01.

Purpose: Consumers need to understand the new fields and see a working code pattern they can copy. The example app must show force vs optional UI so the difference is tangible.
Output: Updated README with usage section, CHANGELOG v2.1.0 entry, example app with force-update path.
</objective>

<execution_context>
@/Users/tguzeldev/Desktop/dev/flutter_desktop_updater/.planning/phases/08-force-and-optional-update-management-via-updateconfig/08-CONTEXT.md
</execution_context>

<context>
@/Users/tguzeldev/Desktop/dev/flutter_desktop_updater/.planning/phases/08-force-and-optional-update-management-via-updateconfig/08-01-SUMMARY.md
@/Users/tguzeldev/Desktop/dev/flutter_desktop_updater/.planning/STATE.md

<interfaces>
<!-- Consumer switch pattern from D-24 — must appear verbatim (or near-verbatim) in README -->
```dart
case UpdateAvailable(:final info):
  if (info.isMandatory) {
    showForceUpdateDialog(info);
  } else {
    showOptionalUpdateBanner(info);
  }
```

<!-- minBuildNumber logic (from CONTEXT.md specifics) — document in README -->
// Server sets minBuildNumber = 15
// User on build 12 → engine sets isMandatory = true  (12 < 15)
// User on build 15 → isMandatory from UpdateSource   (>= min)
// User on build 20 → isMandatory from UpdateSource   (>= min)

<!-- Example app: existing _checkForUpdate method (example/lib/app.dart lines 97-115) -->
// Currently uses UpdateAvailable(:final info) → sets _updateInfo = info
// Must be extended to branch on info.isMandatory
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add force/optional update section to README and CHANGELOG v2.1.0 entry</name>
  <files>README.md, CHANGELOG.md</files>
  <read_first>
    - README.md — full file, to find the right insertion point for the new section
    - CHANGELOG.md — first 60 lines, to understand the existing v2.0.0 format
  </read_first>
  <action>
    README.md — add a new section "### Force vs Optional Updates" after the existing usage/checkForUpdate section. Content must include:

    1. Brief explanation: the engine auto-sets `isMandatory=true` when the device's build number is below `minBuildNumber`; otherwise, the value comes from whatever the `UpdateSource` returns.
    2. Fields table:
       | Field | Type | Default | Description |
       |-------|------|---------|-------------|
       | isMandatory | bool | false | Engine sets this; consumer uses it to drive UI |
       | minBuildNumber | int? | null | Minimum build that can skip the update |
       | releaseNotes | String? | null | Optional notes to show in update UI |
    3. UpdateSource example showing how to set minBuildNumber and releaseNotes when constructing UpdateInfo in getLatestUpdateInfo().
    4. Consumer switch pattern verbatim from D-24 (see interfaces block above).
    5. minBuildNumber logic table (build 12, 15, 20 examples from CONTEXT.md specifics section).

    CHANGELOG.md — insert a new `## 2.1.0` section ABOVE the existing `## 2.0.0` entry. Format:

    ```markdown
    ## 2.1.0

    ### New features

    - `UpdateInfo` gains three optional fields: `isMandatory` (bool, default `false`),
      `minBuildNumber` (int?), and `releaseNotes` (String?). Existing `UpdateSource`
      implementations require no changes — all fields have defaults.
    - `checkForUpdate()` automatically sets `isMandatory = true` when
      `minBuildNumber != null && localBuild < minBuildNumber`. If the `UpdateSource`
      already sets `isMandatory = true`, the engine preserves it.
    - `UpdateInfo.copyWith()` now accepts all three new fields.
    ```
  </action>
  <verify>
    <automated>cd /Users/tguzeldev/Desktop/dev/flutter_desktop_updater && grep -n "isMandatory" README.md && grep -n "2.1.0" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains "isMandatory" in a dedicated section with a code snippet matching D-24 consumer pattern
    - README.md contains "minBuildNumber" with the 3-row build comparison example
    - CHANGELOG.md starts with `## 2.1.0` (before `## 2.0.0`)
    - CHANGELOG.md v2.1.0 section mentions isMandatory, minBuildNumber, releaseNotes, and copyWith
  </acceptance_criteria>
  <done>
    README has a clear force/optional section a consumer can follow. CHANGELOG has a v2.1.0 entry at the top describing all three new fields.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update example app to demonstrate force vs optional update UI</name>
  <files>example/lib/app.dart</files>
  <read_first>
    - example/lib/app.dart — full file (212 lines) to understand all existing state fields and methods
  </read_first>
  <action>
    Update example/lib/app.dart to demonstrate the D-24 consumer pattern.

    CHANGES REQUIRED:

    1. Add `bool _isMandatory = false;` state field alongside `_updateInfo` and `_progress`.

    2. In `_checkForUpdate()`, update the `UpdateAvailable` case to:
       ```dart
       case UpdateAvailable(:final info):
         setState(() {
           _updateInfo = info;
           _isMandatory = info.isMandatory;
           _status = info.isMandatory
               ? 'REQUIRED update: ${info.version} — must install'
               : 'Update available: ${info.version} '
                   '(${info.changedFiles.length} files)';
         });
       ```

    3. In the `build()` method, add an `AlertDialog`-based force-update overlay using `showDialog` rather than an inline widget. The preferred approach: store `_showForceDialog = false` bool state field, set it to `true` when `_isMandatory` is detected in `_checkForUpdate`, and in `didUpdateWidget` or `initState`-like logic show the dialog via `WidgetsBinding.instance.addPostFrameCallback`. However, given the headless example context, a simpler approach is acceptable: render an in-widget `Container` banner at the top of the Column when `_isMandatory && _updateInfo != null` with text "Update required — you must install this update to continue." styled with a red/orange background.

    The example does NOT need a real non-dismissible dialog — a clearly styled mandatory banner in the UI is sufficient to demonstrate the concept. Keep the example simple.

    4. Update the Download button `onPressed` logic: it should be enabled whenever `_updateInfo != null` (mandatory updates should also be downloadable, not gated differently).

    5. Add `/// doc comment` on `_isMandatory` field to satisfy any lint rules active in the example.

    Do NOT change JsonUpdateSource or the server URLs — the source does not need minBuildNumber for the example to compile and run.
  </action>
  <verify>
    <automated>cd /Users/tguzeldev/Desktop/dev/flutter_desktop_updater && flutter analyze example/lib/app.dart</automated>
  </verify>
  <acceptance_criteria>
    - example/lib/app.dart contains `_isMandatory` state field
    - example/lib/app.dart contains `info.isMandatory` check in `_checkForUpdate`
    - flutter analyze produces no errors on example/lib/app.dart (warnings acceptable if pre-existing)
    - The file compiles: `flutter build macos --dry-run` in example/ would succeed (verified via analyze)
  </acceptance_criteria>
  <done>
    Example app shows force vs optional update UI branching, demonstrating the D-24 consumer pattern that README documents.
  </done>
</task>

</tasks>

<verification>
- `grep -n "isMandatory" README.md` returns at least 5 lines
- `grep -n "2.1.0" CHANGELOG.md` returns the new header line
- `flutter analyze example/lib/app.dart` returns no errors
- `flutter test` still passes (documentation changes touch no production logic)
</verification>

<success_criteria>
- README documents the 3 new fields with a working code example matching D-24
- CHANGELOG has v2.1.0 section above v2.0.0 with accurate description of new features
- Example app compiles and demonstrates isMandatory branching in _checkForUpdate
- No test regressions: flutter test passes
</success_criteria>

<output>
After completion, create `.planning/phases/08-force-and-optional-update-management-via-updateconfig/08-02-SUMMARY.md`
</output>
