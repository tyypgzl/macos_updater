name: macos_updater_example
description: Demonstrates how to use the macos_updater plugin.
publish_to: none
version: 2.0.0

environment:
  sdk: '>=3.7.0 <4.0.0'
  flutter: '>=3.29.0'

dependencies:
  macos_updater:
    path: ../
  flutter:
    sdk: flutter
  http: ^1.6.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
